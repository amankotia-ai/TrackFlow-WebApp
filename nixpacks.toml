[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'Node.js setup complete'", "node --version", "npm --version"]

[start]
cmd = "node railway-server.js" 